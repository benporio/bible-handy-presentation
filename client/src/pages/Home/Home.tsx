import { Container } from '@mui/material';
import React from 'react';

interface HomeProps { }

export const Home: React.FC<HomeProps> = () => {
    return (
        <Container>
            Home Page
        </Container>
    );
}